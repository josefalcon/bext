{
  "name": "bext",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "js": "browserify javascript/app.js -t babelify --outfile app.js && browserify javascript/events.js -t babelify --outfile events.js",
    "clean": "rm app.js events.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^6.3.0"
  },
  "dependencies": {
    "react": "^0.13.3"
  }
}
