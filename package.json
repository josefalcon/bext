{
  "name": "bext",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p ext/src && browserify -t [ babelify ] scripts/events.js -o ext/src/events.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "match-pattern": "0.0.1"
  }
}
