{
  "name": "bext",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "js": "mkdir -p src && browserify scripts/components/app.js -t babelify --outfile src/app.js && browserify scripts/events.js -t babelify --outfile src/events.js && browserify scripts/title-observer.js -t babelify --outfile src/title-observer.js",
    "clean": "rm -rf src/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^6.3.0",
    "browserify": "^11.1.0"
  },
  "dependencies": {
    "classnames": "^2.1.3",
    "match-pattern": "^0.0.1",
    "react": "^0.13.3"
  }
}
